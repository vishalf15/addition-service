version: 0.0

phases:
  install:
    commands:
      - echo Nothing to do in the install phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started
      - mvn install
  post_build:
    commands:
      - echo Build completed
artifacts:
  files:
    - target/addition-service-1.0.0.jar